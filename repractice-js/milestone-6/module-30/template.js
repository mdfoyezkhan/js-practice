const a = 40;
const b = 40;
const result = `the sum of number ${a} and ${b} is ${a + b}`;
console.log(result)

const person = {
    name: 'foyez',
    age: 39
}
const friend = 40;
const sum = `the sum of number ${person.age} and ${friend} is ${person.age + friend}`;
console.log(sum)
