const deffrent = (person) => a + b;
const student = {
    name: 'foyej',
    age: 23,
}
const person = (person) => person.age
console.log(person(student))

const sum = (a, b, c) => {
    const x = a + b + c;
    const y = a - b -c;
    const z = a * b * c;
    return x, y, z;
}
const result = sum(12, 12, 12);
console.log(result)